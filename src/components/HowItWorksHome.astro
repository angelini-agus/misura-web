---
import { howWeWork } from "../lib/content";
---

<section id="como-trabajamos" class="scroll-mt-24 border-t border-green">
  <div class="mx-auto max-w-6xl px-4 py-16 md:px-6 md:py-24">
    <div
      class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"
      data-reveal
    >
      <div class="max-w-xl">
        <p class="text-xs font-semibold uppercase tracking-[0.2em]">
          {howWeWork.eyebrow}
        </p>
        <h2
          class="font-display mt-3 text-3xl font-medium leading-tight tracking-tight sm:text-4xl"
        >
          {howWeWork.title}
        </h2>
      </div>
      <p class="max-w-sm leading-relaxed text-green/80">{howWeWork.subtitle}</p>
    </div>

    <ol class="mt-12 grid gap-8 md:grid-cols-2 lg:grid-cols-4">
      {howWeWork.steps.map((step, index) => (
        <li
          class="border-t-2 border-green pt-5"
          data-reveal
          style={{ transitionDelay: `${index * 60}ms` }}
        >
          <span class="font-mono text-xs uppercase tracking-widest text-green/75">
            {`Paso ${index + 1}`}
          </span>
          <h3 class="font-display mt-2 text-lg font-medium">{step.title}</h3>
          <p class="mt-2 text-sm leading-relaxed text-green/80">
            {step.description}
          </p>
        </li>
      ))}
    </ol>
  </div>
</section>
